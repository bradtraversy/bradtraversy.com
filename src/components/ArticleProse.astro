<div class="article-prose">
	<slot />
</div>

<style>
	.article-prose {
		width: min(calc(100% - 3rem), 49.375rem);
		margin-inline: auto;
		padding-block: 5.75rem 7rem;
	}

	.article-prose > :global(p) {
		margin: 0 0 1.75rem;
		color: #c4beb1;
		font-size: 1.125rem;
		line-height: 1.86;
	}

	.article-prose > :global(p:first-child) {
		color: var(--color-bone);
		font-size: 1.375rem;
		line-height: 1.72;
	}

	.article-prose > :global(h2) {
		margin: 5.125rem 0 1.625rem;
		color: var(--color-bone);
		font-size: clamp(2.375rem, 5vw, 3.5rem);
		font-weight: 500;
		letter-spacing: -0.052em;
		line-height: 1.05;
	}

	.article-prose > :global(h3) {
		margin: 3rem 0 1rem;
		color: var(--color-bone);
		font-size: 1.375rem;
		font-weight: 600;
		letter-spacing: -0.025em;
		line-height: 1.3;
	}

	.article-prose :global(strong) {
		color: var(--color-bone);
		font-weight: 600;
	}

	.article-prose :global(:not(pre) > code) {
		padding: 0.125rem 0.375rem;
		background: rgba(244, 239, 227, 0.08);
		color: var(--color-gold-soft);
		font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
		font-size: 0.86em;
	}

	.article-prose > :global(pre) {
		overflow-x: auto;
		margin: 2.5rem 0 3.125rem;
		padding: 1.625rem 1.75rem;
		border: 1px solid var(--color-line);
		background: rgba(20, 16, 11, 0.72) !important;
		color: #c4beb1 !important;
		font-size: 0.8125rem;
		line-height: 1.9;
	}

	.article-prose > :global(pre code) {
		padding: 0;
		background: transparent;
		color: inherit;
		font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
	}

	.article-prose > :global(ol) {
		margin: 2.625rem 0 3.375rem;
		padding: 0;
		border-top: 1px solid var(--color-line);
		counter-reset: article-option;
		list-style: none;
	}

	.article-prose > :global(ol > li) {
		position: relative;
		min-height: 4rem;
		padding: 1.5rem 0 1.5rem 4rem;
		border-bottom: 1px solid var(--color-line);
		color: var(--color-muted);
		counter-increment: article-option;
		font-size: 0.875rem;
		line-height: 1.7;
	}

	.article-prose > :global(ol > li::before) {
		position: absolute;
		top: 1.6875rem;
		left: 0;
		color: var(--color-gold);
		content: counter(article-option, decimal-leading-zero);
		font-size: 0.625rem;
		font-weight: 700;
		letter-spacing: 0.12em;
	}

	.article-prose > :global(ol > li p) {
		margin: 0;
	}

	.article-prose > :global(ol > li strong) {
		display: block;
		margin-bottom: 0.5rem;
		font-size: 1.125rem;
	}

	.article-prose > :global(blockquote) {
		margin: 4.75rem 0;
		padding: 1.875rem 0 1.875rem 2rem;
		border-left: 2px solid var(--color-gold);
		color: var(--color-bone);
		font-size: clamp(1.5rem, 3.2vw, 2.125rem);
		letter-spacing: -0.035em;
		line-height: 1.4;
	}

	.article-prose > :global(blockquote p) {
		margin: 0 0 1.125rem;
	}

	.article-prose > :global(blockquote p:last-child) {
		margin-bottom: 0;
		color: var(--color-gold-soft);
	}

	@media (max-width: 47.5rem) {
		.article-prose {
			width: min(calc(100% - 2.5rem), 49.375rem);
			padding-block: 4.125rem 5.125rem;
		}

		.article-prose > :global(p) {
			font-size: 1.0625rem;
		}

		.article-prose > :global(p:first-child) {
			font-size: 1.25rem;
		}

		.article-prose > :global(h2) {
			margin-top: 4rem;
		}

		.article-prose > :global(pre) {
			margin-inline: -0.25rem;
			padding: 1.25rem;
			font-size: 0.75rem;
		}

		.article-prose > :global(ol > li) {
			padding-left: 3.25rem;
		}

		.article-prose > :global(blockquote) {
			padding-left: 1.375rem;
		}
	}
</style>
