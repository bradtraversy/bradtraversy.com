<a
	class="project-archive shell"
	href="https://bradtraversy.dev/projects"
	target="_blank"
	rel="noreferrer"
>
	<div>
		<p class="project-archive__kicker">The technical trail</p>
		<h2>See every project, decision, and build log.</h2>
		<p>
			This page stays curated. The full project archive, recent changes, stack details,
			and in-progress work live on bradtraversy.dev.
		</p>
	</div>
	<span class="project-archive__arrow" aria-hidden="true">→</span>
</a>

<style>
	.project-archive {
		display: grid;
		grid-template-columns: 1fr auto;
		align-items: center;
		gap: 3rem;
		padding-block: 4.5rem;
		text-decoration: none;
	}

	.project-archive__kicker {
		margin: 0 0 1.5rem;
		color: var(--color-gold);
		font-size: 0.75rem;
		font-weight: 700;
		letter-spacing: 0.2em;
		text-transform: uppercase;
	}

	h2 {
		max-width: 51.25rem;
		margin: 0 0 1.125rem;
		font-size: clamp(2.625rem, 5vw, 4.5rem);
		font-weight: 500;
		letter-spacing: -0.055em;
		line-height: 1;
	}

	p:not(.project-archive__kicker) {
		max-width: 42.5rem;
		margin: 0;
		color: var(--color-muted);
	}

	.project-archive__arrow {
		display: grid;
		width: 4.75rem;
		height: 4.75rem;
		place-items: center;
		border: 1px solid var(--color-line);
		color: var(--color-gold);
		font-size: 1.625rem;
		transition:
			border-color 180ms ease,
			transform 180ms ease;
	}

	.project-archive:hover .project-archive__arrow,
	.project-archive:focus-visible .project-archive__arrow {
		border-color: var(--color-gold);
		transform: translateX(0.3125rem);
	}

	@media (max-width: 40rem) {
		.project-archive {
			grid-template-columns: 1fr;
			gap: 1.875rem;
		}

		.project-archive__arrow {
			width: 3.625rem;
			height: 3.625rem;
		}
	}
</style>
