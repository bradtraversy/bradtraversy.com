<section class="about section shell" id="about" aria-labelledby="about-title">
	<div class="about__grid">
		<div>
			<p class="about__kicker">About</p>
			<h2 id="about-title">Built through experience.</h2>
		</div>
		<p class="about__copy">
			I'm a developer and educator from the Boston area. For more than a decade I've
			helped people learn web development while building products of my own.
			<strong>This site brings the best of that work together.</strong>
		</p>
	</div>
</section>

<style>
	.section {
		padding-block: 8.25rem;
	}

	.about__grid {
		display: grid;
		grid-template-columns: 0.72fr 1.28fr;
		gap: 7.5rem;
	}

	.about__kicker {
		margin: 0 0 1.5rem;
		color: var(--color-gold);
		font-size: 0.75rem;
		font-weight: 700;
		letter-spacing: 0.2em;
		text-transform: uppercase;
	}

	h2 {
		max-width: 45rem;
		margin: 0;
		font-size: clamp(2.625rem, 5vw, 4.625rem);
		font-weight: 500;
		letter-spacing: -0.05em;
		line-height: 1;
	}

	.about__copy {
		max-width: 47.5rem;
		margin: 0;
		color: var(--color-muted);
		font-size: clamp(1.1875rem, 2vw, 1.6875rem);
		line-height: 1.55;
	}

	.about__copy strong {
		color: var(--color-bone);
		font-weight: 500;
	}

	@media (max-width: 56.25rem) {
		.about__grid {
			grid-template-columns: 1fr;
			gap: 2.5rem;
		}
	}

	@media (max-width: 40rem) {
		.section {
			padding-block: 5.5rem;
		}
	}
</style>
